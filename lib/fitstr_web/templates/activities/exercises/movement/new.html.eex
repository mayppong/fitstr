<h2>New Movement</h2>

<%= render "form.html", Map.put(assigns, :action, activities_exercises_movement_path(@conn, :create)) %>

<span><%= link "Back", to: activities_exercises_movement_path(@conn, :index) %></span>
