<h2>Edit Workout</h2>

<%= render "form.html", Map.put(assigns, :action, activities_workouts_workout_path(@conn, :update, @workout)) %>

<span><%= link "Back", to: activities_workouts_workout_path(@conn, :index) %></span>
