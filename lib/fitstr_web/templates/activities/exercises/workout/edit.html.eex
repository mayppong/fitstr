<h2>Edit Workout</h2>

<%= render "form.html", Map.put(assigns, :action, activities_exercises_workout_path(@conn, :update, @workout)) %>

<span><%= link "Back", to: activities_exercises_workout_path(@conn, :index) %></span>
